global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: "pitun"
    static_configs:
      - targets: ["host.docker.internal:8000"]
        labels:
          instance: "pitun"
    metrics_path: /metrics
